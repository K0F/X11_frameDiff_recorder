#X11 frame-diff recorder

Program dumps X11 pixels and shm-copies them into ffmpeg pipe. It uses pulseaudio detection to capture audio as well.

 ## build
 ```bash
 make
 ./gl_overlay
 ```

 ## run
 ```bash
 ./gl_overlay
 ```
  
